import React from 'react'

function Person(props) {
    return (
        <div >
            
                I am {props.studentData.name}. {props.studentData.rollno} 
            
        </div>
    )
}

export default Person
